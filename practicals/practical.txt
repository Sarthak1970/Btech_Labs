//Ask the user to enter n(no of element) . Input n integer and find the largest number in the array.

//Selective Repeat simulation. Enter no of frames ,loss prob ,window size and specific frames to drop , Retransmit only the lost frame and maintain receiver buffer and out of order frames

